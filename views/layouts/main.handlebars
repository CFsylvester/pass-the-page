<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fancy New App Name!!!</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Goudy+Bookletter+1911&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <header >
        <nav class="container">
           <div class="row">
               <div class="col-4">
                   empty
               </div>
                <div class="col-4">
                    <a href="/">
                        <img src='/src/logo.png' width="400" height="100" alt="" loading="lazy">
                    </a>
                </div>
                {{#if loggedIn}}
                <div>
                    <a href="/dashboard"><button class='btn btn-primary'>Dashboard</button></a>
                </div>
                <div>
                    <a href="/login"><button id='logout-btn' class='btn btn-danger'>Logout</button></a>
                </div>
                {{else}}
                {{!-- <div>
                    <a href="/signup"><button class='btn btn-success'>Create Account</button></a>
                </div> --}}
                <div class="col-4">
                    <a href="/login">
                        <button class='btn'>Login</button>
                    </a>
                </div>
                {{/if}}
           </div>
        </nav>
    </header>
    <main>
        {{{ body }}}
    </main>
    <footer class='footer'>
        <div>
            <p>Thanks for visiting!</p>
        </div>
        <div>
            <p>Created by: Claire Sylvester, Curtis Henderson & Tom Chestnut</p>
        </div>
    </footer>

    {{#if loggedIn}}
    <script src='/javascript/logout.js'></script>
    {{/if}}
</body>

</html>